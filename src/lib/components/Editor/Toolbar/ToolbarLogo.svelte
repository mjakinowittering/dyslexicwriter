<script lang="ts">
    import { Brain02Icon } from '@hugeicons/core-free-icons';

    import Icon from '$lib/components/Icon/Icon.svelte';

    // The logo column, immediately right of the rail and spanning the same full
    // height (the title row AND the toolbar row). Decorative only — no button,
    // no tooltip: a two-route app gives the mark nowhere to navigate.
</script>

<div class="flex shrink-0 items-center justify-center pl-3">
    <Icon class="text-primary size-8" icon={Brain02Icon} />
</div>
